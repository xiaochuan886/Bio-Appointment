# 护士资源自动同步实现说明

## 功能概述

当在用户管理页面添加、更新或停用护士用户时，系统会自动在 `resources` 表中同步创建或更新对应的资源记录，确保数据一致性。

## 实现方式

使用 PostgreSQL 数据库触发器（Trigger）实现自动同步，无需修改应用层代码。

### 触发器逻辑

**文件**: `database/migrations/11-auto-sync-nurse-resources.sql`

#### 触发条件
- 在 `profiles` 表中 **插入** 或 **更新** 记录时
- 监控字段：`role`, `status`, `full_name`, `store_id`

#### 同步规则

1. **创建护士用户** (`role` = 'nurse' 或 'head_nurse', `status` = 'active')
   - 自动在 `resources` 表中创建对应记录
   - 资源类型：`type = 'nurse'`
   - 资源状态：`status = 'available'`
   - 门店关联：`store_id` 与用户一致

2. **更新护士信息** (姓名、门店等)
   - 自动更新 `resources` 表中的对应记录
   - 保持数据同步

3. **停用护士** (`status` = 'disabled')
   - 自动将资源状态更新为 `status = 'unavailable'`
   - 该护士将不再出现在可用资源列表中

4. **重新激活护士** (`status` = 'active')
   - 自动将资源状态恢复为 `status = 'available'`
   - 该护士重新可用于排班

## 测试验证

### 测试场景

✅ **场景1：创建新护士**
```sql
INSERT INTO profiles (username, email, full_name, role, status, store_id)
VALUES ('nurse_new', 'nurse@example.com', '新护士', 'nurse', 'active', '<store_id>');
```
结果：自动在 `resources` 表中创建对应记录

✅ **场景2：更新护士门店**
```sql
UPDATE profiles SET store_id = '<new_store_id>' WHERE id = '<nurse_id>';
```
结果：资源记录的 `store_id` 自动同步更新

✅ **场景3：停用护士**
```sql
UPDATE profiles SET status = 'disabled' WHERE id = '<nurse_id>';
```
结果：资源状态自动更新为 'unavailable'

✅ **场景4：重新激活护士**
```sql
UPDATE profiles SET status = 'active' WHERE id = '<nurse_id>';
```
结果：资源状态自动恢复为 'available'

### 测试脚本

运行 `test-nurse-resource-sync.cjs` 进行完整测试：
```bash
node test-nurse-resource-sync.cjs
```

## 数据库表结构

### profiles 表（用户表）
```sql
profiles
├── id (UUID)
├── username
├── email
├── full_name
├── role (nurse, head_nurse, doctor, sales, etc.)
├── status (active, disabled)
├── store_id (关联门店)
└── ...
```

### resources 表（资源表）
```sql
resources
├── id (UUID)
├── name (对应 profiles.full_name)
├── type (nurse, room, vip, treatment, consultation)
├── status (available, unavailable)
├── store_id (关联门店)
└── ...
```

### schedules 表（排班表）
```sql
schedules
├── id (UUID)
├── nurse_id → profiles.id (注意：关联到 profiles，不是 resources)
├── room_id → resources.id
└── ...
```

## 重要说明

### 为什么需要 resources 表中的护士记录？

虽然当前的资源可用性检查直接从 `profiles` 表查询护士，但保持 `resources` 表中的护士记录有以下好处：

1. **数据完整性**：统一管理所有可调度资源（房间和护士）
2. **未来扩展性**：便于实现更复杂的资源管理功能
3. **报表统计**：方便生成资源使用率报告
4. **历史兼容**：某些旧代码可能依赖 `resources` 表

### 触发器 vs 应用层同步

**选择触发器的原因**：
- ✅ 数据库层面保证一致性，无论通过哪个入口修改数据
- ✅ 无需修改现有应用代码
- ✅ 性能开销小
- ✅ 事务安全

**注意事项**：
- 触发器在数据库层面执行，应用层无感知
- 如果直接操作数据库，触发器仍会生效
- 触发器逻辑应保持简单，避免复杂业务逻辑

## 迁移说明

### 应用迁移

迁移文件已创建：`database/migrations/11-auto-sync-nurse-resources.sql`

执行迁移：
```bash
cat database/migrations/11-auto-sync-nurse-resources.sql | \
  docker exec -i bio-appointment-postgres psql -U app_user -d bio_appointment
```

### 初始化现有数据

迁移脚本会自动为所有现有的活跃护士创建资源记录：
```sql
INSERT INTO resources (name, type, status, store_id)
SELECT 
  p.full_name,
  'nurse' as type,
  'available' as status,
  p.store_id
FROM profiles p
WHERE p.role IN ('nurse', 'head_nurse')
  AND p.status = 'active'
  AND NOT EXISTS (
    SELECT 1 FROM resources r
    WHERE r.name = p.full_name AND r.type = 'nurse'
  );
```

## 监控和维护

### 检查同步状态

查看护士用户和资源记录是否一致：
```sql
-- 查看所有护士用户
SELECT id, full_name, role, status, store_id
FROM profiles
WHERE role IN ('nurse', 'head_nurse')
ORDER BY full_name;

-- 查看所有护士资源
SELECT id, name, type, status, store_id
FROM resources
WHERE type = 'nurse'
ORDER BY name;

-- 查找不一致的记录
SELECT p.full_name, p.status as user_status, r.status as resource_status
FROM profiles p
LEFT JOIN resources r ON p.full_name = r.name AND r.type = 'nurse'
WHERE p.role IN ('nurse', 'head_nurse')
  AND (r.id IS NULL OR p.status != r.status);
```

### 手动修复不一致

如果发现数据不一致，可以手动触发同步：
```sql
-- 重新同步所有护士资源
UPDATE profiles 
SET updated_at = CURRENT_TIMESTAMP 
WHERE role IN ('nurse', 'head_nurse');
```

## 相关文件

- `database/migrations/11-auto-sync-nurse-resources.sql` - 触发器定义
- `test-nurse-resource-sync.cjs` - 测试脚本
- `server/api-server.cjs` - 资源可用性API（使用 profiles 表查询护士）
- `资源可用性检查修复报告.md` - 相关功能说明

## 总结

通过数据库触发器实现护士资源的自动同步，确保了：
- ✅ 数据一致性：用户表和资源表始终保持同步
- ✅ 零代码侵入：无需修改应用层代码
- ✅ 自动化：创建、更新、停用护士时自动处理
- ✅ 可靠性：数据库层面保证，事务安全

现在管理员可以放心地在用户管理页面添加新护士，系统会自动处理资源同步。
