# 服务删除功能修复说明

## 问题描述

在服务项目管理中，当尝试删除一个被预约使用的基础回输服务时，系统返回了不友好的错误信息："Cannot delete service"，用户无法理解具体原因和解决方案。

## 根本原因

服务器端的删除服务API检查服务是否被任何预约使用，如果被使用，则拒绝删除请求。但是返回的错误信息不够详细，没有说明：
1. 服务被多少个预约使用
2. 哪些预约正在使用该服务
3. 用户应该如何处理这种情况

## 修复内容

### 1. 服务器端改进 (`server/api-server.cjs`)

**修改位置**: 第 444-455 行

**改进内容**:
- 提供更详细的错误信息，包括：
  - 服务被多少个预约使用
  - 使用该服务的预约详情（客户名称和日期）
  - 具体的解决建议

**改进前**:
```javascript
return res.status(400).json({
  error: 'Cannot delete service',
  message: 'Service is being used by existing appointments'
});
```

**改进后**:
```javascript
// 获取使用该服务的预约详情
const appointmentDetails = await pool.query(
  'SELECT id, customer_name, requested_date FROM appointments WHERE service_id = $1 LIMIT 3',
  [id]
);

const appointmentList = appointmentDetails.rows.map(apt => 
  `- ${apt.customer_name} (${apt.requested_date})`
).join('\n');

return res.status(400).json({
  error: 'Cannot delete service',
  message: `该服务被 ${appointmentCheck.rows[0].count} 个预约使用，无法删除。\n\n使用该服务的预约：\n${appointmentList}\n\n建议：\n1. 将服务状态设置为"禁用"而不是删除\n2. 先删除或修改使用该服务的预约`,
  appointmentCount: parseInt(appointmentCheck.rows[0].count),
  appointments: appointmentDetails.rows
});
```

### 2. 前端改进 (`src/pages/admin/SystemConfigPage.tsx`)

#### 2.1 增强错误处理

**修改位置**: 第 224-234 行

**改进内容**:
- 更好地处理服务被使用的情况
- 显示更友好的错误提示

#### 2.2 添加禁用服务功能

**修改位置**: 第 849-867 行

**新增功能**:
- 添加禁用/启用服务的按钮
- 允许用户在不删除服务的情况下将其禁用
- 禁用的服务不会出现在新的预约选择中，但历史记录保留

**新增函数**:
```typescript
const handleToggleServiceStatus = async (service: Service) => {
  const newStatus = !service.is_active;
  const action = newStatus ? '启用' : '禁用';
  
  if (!confirm(`确定要${action}服务"${service.name}"吗？`)) return;
  
  try {
    await clientApi.updateService(service.id, { is_active: newStatus });
    toast.success(`服务${action}成功`);
    loadData();
  } catch (error: any) {
    toast.error(error.message || `${action}失败`);
  }
};
```

#### 2.3 用户界面改进

**修改位置**: 第 535-540 行

**改进内容**:
- 添加提示信息，说明禁用服务的用途
- 在表格中添加禁用按钮图标

## 测试验证

### 1. 删除服务测试

创建了测试脚本 `test-delete-service-fix.cjs` 验证：
- 服务器返回详细的错误信息
- 包含预约数量和详情
- 提供解决建议

**测试结果**: ✅ 通过

### 2. 禁用服务测试

创建了测试脚本 `test-disable-service.cjs` 验证：
- 服务状态可以正常切换
- 数据库状态正确更新
- 可以恢复原始状态

**测试结果**: ✅ 通过

## 解决方案总结

### 短期解决方案（已实现）
1. **改进错误信息**: 提供详细的错误说明，包括预约数量和详情
2. **添加禁用功能**: 允许用户禁用服务而不是删除，保留历史记录
3. **用户友好提示**: 在界面上添加说明，指导用户如何处理这种情况

### 长期建议
1. **软删除机制**: 实现软删除，标记服务为已删除但保留历史记录
2. **服务依赖检查**: 在删除前显示完整的依赖关系
3. **批量操作**: 允许批量禁用或重新分配服务

## 使用指南

### 对于用户
1. **删除服务**: 如果服务未被任何预约使用，可以直接删除
2. **禁用服务**: 如果服务正在被使用，点击禁用按钮将其标记为不可用
3. **查看详情**: 错误消息会显示使用该服务的预约详情

### 对于开发者
1. **错误处理**: 前端现在可以区分不同类型的删除错误
2. **状态管理**: 服务状态可以在启用和禁用之间切换
3. **用户体验**: 提供了更友好的错误提示和解决建议

## 文件修改清单

1. `server/api-server.cjs` - 改进删除服务的错误信息
2. `src/pages/admin/SystemConfigPage.tsx` - 添加禁用功能和改进错误处理
3. `check-service-usage.cjs` - 服务使用情况检查脚本
4. `test-delete-service-fix.cjs` - 删除服务测试脚本
5. `test-disable-service.cjs` - 禁用服务测试脚本
6. `服务删除功能修复说明.md` - 本文档

## 总结

通过这次修复，我们解决了服务删除功能中的用户体验问题：
- 提供了清晰的错误信息
- 添加了禁用服务的替代方案
- 保留了历史记录的完整性
- 改善了用户界面的友好性

用户现在可以更好地理解为什么无法删除服务，以及如何处理这种情况。