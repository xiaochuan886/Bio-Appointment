# Bio-Appointment 用户认证与权限管理系统使用指南

## 系统概述

Bio-Appointment 现已集成完整的用户身份认证与权限管理系统，实现了基于角色的访问控制（RBAC），确保不同身份的用户拥有不同的系统访问和操作权限。

## 系统角色

### 1. 超级管理员（super_admin）
- **权限范围**：系统最高权限
- **主要功能**：
  - 管理所有用户账号
  - 分配和修改用户角色
  - 启用/禁用用户账号
  - 访问所有功能模块
  - 系统配置管理

### 2. 销售/健康管理师（sales）
- **权限范围**：客户预约管理
- **主要功能**：
  - 发起新预约
  - 查看自己创建的预约
  - 跟进预约状态
  - 查看相关排班信息

### 3. 护士长（head_nurse）
- **权限范围**：排班和资源管理
- **主要功能**：
  - 智能排班
  - 资源分配（房间、护士）
  - 查看所有预约
  - 管理护士任务
  - 确认和发布排班

### 4. 护士（nurse）
- **权限范围**：任务执行
- **主要功能**：
  - 查看分配给自己的任务
  - 执行护理任务
  - 更新任务状态
  - 记录任务完成情况

### 5. 医生（doctor）
- **权限范围**：预约确认
- **主要功能**：
  - 查看分配给自己的预约
  - 接受/拒绝预约
  - 申请改期
  - 添加医生备注

## 使用流程

### 首次使用

1. **注册第一个账号**
   - 访问系统注册页面
   - 填写用户名、密码等信息
   - 第一个注册的用户将自动成为超级管理员

2. **创建其他用户**
   - 超级管理员登录后，访问"用户管理"页面
   - 其他用户可以自行注册（默认角色为销售）
   - 超级管理员可以为用户分配合适的角色

### 日常使用

1. **登录系统**
   - 使用用户名和密码登录
   - 系统会根据角色显示相应的功能菜单

2. **角色切换**
   - 如需更改用户角色，请联系超级管理员
   - 超级管理员在"用户管理"页面进行角色分配

3. **权限说明**
   - 每个角色只能访问其权限范围内的功能
   - 尝试访问无权限页面会显示"访问受限"提示

## 安全特性

### 认证机制
- 使用 Supabase Auth 进行安全认证
- 密码加密存储
- Session 管理和自动刷新
- 登录状态持久化

### 权限控制
- **模块级权限**：控制用户能否访问特定功能模块
- **操作级权限**：控制用户在模块内可执行的具体操作
- **数据级权限**：通过 RLS（Row Level Security）控制数据访问范围

### 数据隔离
- 销售只能查看自己创建的预约
- 护士只能查看分配给自己的任务
- 医生只能查看分配给自己的预约
- 超级管理员和护士长可以查看所有数据

## 常见问题

### Q: 忘记密码怎么办？
A: 请联系系统管理员重置密码。

### Q: 如何申请更高权限？
A: 请联系超级管理员，说明需求后由管理员分配相应角色。

### Q: 为什么看不到某些菜单？
A: 系统会根据您的角色动态显示菜单，您只能看到有权限访问的功能。

### Q: 账号被禁用了怎么办？
A: 请联系超级管理员了解原因并申请重新启用。

### Q: 可以同时拥有多个角色吗？
A: 当前系统设计为每个用户只有一个角色，如需多角色权限，请联系管理员分配超级管理员角色。

## 技术实现

### 数据库层
- 使用 PostgreSQL 枚举类型定义角色
- 通过 RLS 策略实现行级安全控制
- 自动触发器同步用户信息

### 应用层
- React Context 管理认证状态
- 路由守卫保护需要权限的页面
- API 层面的权限验证

### 前端展示
- 根据角色动态生成导航菜单
- 权限不足时显示友好提示
- 实时更新用户状态

## 开发者注意事项

1. **添加新角色**：需要修改数据库枚举类型和相关类型定义
2. **添加新权限**：需要更新 RLS 策略和路由配置
3. **修改权限逻辑**：需要同时更新数据库策略和前端路由守卫
4. **测试权限**：建议创建不同角色的测试账号进行完整测试

## 更新日志

### v1.0.0 (2025-01-27)
- ✅ 实现完整的用户认证系统
- ✅ 实现基于角色的权限控制
- ✅ 创建用户管理界面
- ✅ 实现动态菜单生成
- ✅ 添加数据级权限控制
- ✅ 集成登录/注册/登出功能
