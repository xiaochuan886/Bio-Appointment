# 登录问题排查指南

## 🐛 问题描述

点击登录后一直显示"登录中..."，无法正确进入系统。

## 🔍 已实施的修复

### 1. 添加详细日志

在以下文件中添加了详细的控制台日志：

- ✅ `src/db/api.ts` - login 函数
- ✅ `src/db/api.ts` - register 函数
- ✅ `src/pages/auth/LoginPage.tsx` - 登录页面
- ✅ `src/pages/auth/RegisterPage.tsx` - 注册页面
- ✅ `src/contexts/AuthContext.tsx` - 认证上下文

### 2. 改进错误处理

- ✅ 所有 API 调用都包裹在 try-catch 中
- ✅ 详细的错误信息输出
- ✅ 用户友好的错误提示

### 3. 添加超时保护

- ✅ AuthContext 有 5 秒超时机制
- ✅ 防止无限加载

## 📋 排查步骤

### 步骤 1：检查是否有测试用户

**问题：** 数据库中可能没有用户数据

**解决方法：**

1. **通过注册创建用户（推荐）**
   ```
   1. 访问注册页面（点击"立即注册"）
   2. 填写信息：
      - 用户名：admin
      - 密码：123456
      - 真实姓名：管理员（可选）
   3. 点击"注册"按钮
   4. 注册成功后会跳转到登录页
   ```

2. **查看注册日志**
   - 打开浏览器开发者工具（F12）
   - 切换到 Console 标签
   - 观察注册过程的日志输出

**预期日志：**
```
=== 开始注册流程 ===
表单值: { username: 'admin', full_name: '管理员', password: '***' }
1. 调用 register API...
开始注册流程，用户名: admin
尝试使用邮箱注册: admin@miaoda.com
注册成功，用户 ID: xxx-xxx-xxx
2. 注册 API 返回结果: { ... }
=== 注册成功 ===
```

---

### 步骤 2：尝试登录并观察日志

**操作：**

1. 在登录页面输入刚才注册的用户名和密码
2. 打开浏览器开发者工具（F12）
3. 切换到 Console 标签
4. 点击"登录"按钮
5. 仔细观察控制台输出

**预期日志（成功情况）：**
```
=== 开始登录流程 ===
表单值: { username: 'admin', password: '***' }
1. 调用 login API...
开始登录流程，用户名: admin
尝试使用邮箱登录: admin@miaoda.com
登录成功，用户 ID: xxx-xxx-xxx
获取 profile 成功: { id: '...', username: 'admin', role: 'super_admin', ... }
2. 登录 API 返回结果: { hasUser: true, hasSession: true, hasProfile: true }
3. 刷新用户信息...
4. 显示成功提示...
5. 跳转到首页...
=== 登录流程完成 ===
```

**如果卡在某一步：**

#### 卡在步骤 1（调用 login API）

**可能原因：**
- 网络连接问题
- Supabase 配置错误

**检查方法：**
1. 查看 Network 标签
2. 检查是否有请求发出
3. 查看请求状态（Pending/Failed/Success）

**解决方法：**
```bash
# 检查 .env 文件
cat .env

# 确认以下配置存在：
# VITE_SUPABASE_URL=...
# VITE_SUPABASE_ANON_KEY=...
```

#### 卡在步骤 2（登录 API 返回）

**可能原因：**
- 用户名或密码错误
- 用户不存在
- Profile 数据缺失

**检查方法：**
1. 查看控制台错误信息
2. 确认用户名和密码正确
3. 检查是否有 "Supabase 登录错误" 的日志

**解决方法：**
- 如果提示 "Invalid login credentials"：用户名或密码错误
- 如果提示 "获取 profile 失败"：数据库表结构问题
- 重新注册一个新用户

#### 卡在步骤 3（刷新用户信息）

**可能原因：**
- AuthContext 初始化问题
- getCurrentUser 函数失败

**检查方法：**
1. 查看是否有 "刷新用户信息失败" 的日志
2. 查看 AuthContext 的日志

**解决方法：**
- 刷新页面重试
- 清除浏览器缓存
- 检查数据库连接

#### 卡在步骤 5（跳转到首页）

**可能原因：**
- 路由配置问题
- 权限验证失败

**检查方法：**
1. 查看是否有路由相关错误
2. 检查用户角色和权限

**解决方法：**
- 检查 routes.tsx 配置
- 确认用户有访问首页的权限

---

### 步骤 3：检查 Network 请求

**操作：**

1. 打开开发者工具（F12）
2. 切换到 Network 标签
3. 点击登录按钮
4. 观察网络请求

**关键请求：**

1. **POST /auth/v1/token?grant_type=password**
   - 这是登录请求
   - 状态应该是 200
   - 响应应该包含 access_token

2. **GET /rest/v1/profiles?id=eq.xxx**
   - 这是获取 profile 的请求
   - 状态应该是 200
   - 响应应该包含用户信息

**如果请求失败：**

- **状态 400**：请求参数错误
- **状态 401**：认证失败（用户名或密码错误）
- **状态 403**：权限不足
- **状态 500**：服务器错误
- **Pending**：请求超时或网络问题

---

### 步骤 4：检查数据库

**通过 Supabase 管理界面：**

1. 访问 Supabase 项目管理界面
2. 进入 Authentication > Users
3. 确认用户存在
4. 进入 Table Editor > profiles
5. 确认该用户有对应的 profile 记录

**必需字段：**
- `id`：与 auth.users 的 id 一致
- `username`：用户名
- `role`：用户角色
- `status`：应该是 'active'

---

## 🔧 常见问题和解决方法

### 问题 1：提示 "Invalid login credentials"

**原因：** 用户名或密码错误，或用户不存在

**解决方法：**
1. 确认用户名和密码正确
2. 尝试重新注册
3. 检查数据库中是否有该用户

### 问题 2：提示 "获取 profile 失败"

**原因：** 用户没有 profile 记录

**解决方法：**
1. 检查数据库迁移是否执行
2. 检查触发器是否正常工作
3. 手动在 profiles 表中创建记录

### 问题 3：登录成功但一直加载

**原因：** AuthContext 刷新用户信息失败

**解决方法：**
1. 查看 AuthContext 的日志
2. 检查 getCurrentUser 函数
3. 等待 5 秒超时后会自动停止

### 问题 4：登录成功但跳转失败

**原因：** 路由配置或权限问题

**解决方法：**
1. 检查控制台错误
2. 确认用户角色正确
3. 检查路由配置

### 问题 5：网络请求一直 Pending

**原因：** 网络连接问题或 Supabase 服务问题

**解决方法：**
1. 检查网络连接
2. 检查 Supabase 配置
3. 尝试刷新页面

---

## 📊 诊断清单

使用以下清单逐项检查：

### 环境配置
- [ ] `.env` 文件存在
- [ ] `VITE_SUPABASE_URL` 已配置
- [ ] `VITE_SUPABASE_ANON_KEY` 已配置
- [ ] 控制台没有 "Supabase 配置缺失" 错误

### 用户数据
- [ ] 已创建测试用户（通过注册）
- [ ] 用户在 auth.users 表中存在
- [ ] 用户在 profiles 表中有记录
- [ ] 用户状态为 'active'

### 登录流程
- [ ] 可以访问登录页面
- [ ] 可以输入用户名和密码
- [ ] 点击登录后有日志输出
- [ ] 没有红色错误信息
- [ ] 5 秒内完成或失败

### 网络请求
- [ ] 登录请求发出（POST /auth/v1/token）
- [ ] 登录请求成功（状态 200）
- [ ] Profile 请求发出（GET /rest/v1/profiles）
- [ ] Profile 请求成功（状态 200）

### 认证状态
- [ ] AuthContext 初始化成功
- [ ] 用户信息获取成功
- [ ] Session 创建成功
- [ ] 可以跳转到首页

---

## 🎯 快速测试脚本

在浏览器控制台运行以下脚本进行快速测试：

### 测试 1：检查 Supabase 连接

```javascript
// 测试 Supabase 连接
const { data, error } = await supabase.auth.getSession();
console.log('Session:', data);
console.log('Error:', error);
```

### 测试 2：测试登录

```javascript
// 测试登录（替换为实际的用户名和密码）
const { data, error } = await supabase.auth.signInWithPassword({
  email: 'admin@miaoda.com',
  password: '123456'
});
console.log('Login result:', data);
console.log('Login error:', error);
```

### 测试 3：查询 Profile

```javascript
// 查询 profile（替换为实际的用户 ID）
const { data, error } = await supabase
  .from('profiles')
  .select('*')
  .eq('id', 'user-id-here')
  .single();
console.log('Profile:', data);
console.log('Error:', error);
```

---

## ✅ 成功标准

登录功能正常的标准：

1. ✅ 可以成功注册新用户
2. ✅ 可以使用用户名和密码登录
3. ✅ 登录过程在 5 秒内完成
4. ✅ 登录成功后跳转到首页
5. ✅ 可以看到用户信息
6. ✅ 控制台没有严重错误

---

## 📞 下一步

如果按照以上步骤仍然无法解决问题：

1. **收集信息：**
   - 完整的控制台日志（从点击登录到结束）
   - Network 标签中的请求详情
   - 错误截图

2. **提供详细描述：**
   - 在哪一步卡住了
   - 具体的错误信息
   - 已经尝试的解决方法

3. **检查数据库：**
   - 确认数据库迁移已执行
   - 确认表结构正确
   - 确认 RLS 策略正确

---

**重要提示：** 请先尝试注册一个新用户，然后使用该用户登录。第一个注册的用户会自动成为超级管理员。
