# 数据库清理指南

## 概述

`clean-test-data.cjs` 脚本用于清理数据库中的所有测试数据，仅保留初始用户和基础配置。

## 保留的初始用户

脚本会保留以下 7 个初始用户：

| 用户名 | 角色 | 部门 | 说明 |
|--------|------|------|------|
| admin | super_admin | 管理部 | 系统管理员 |
| sales1 | sales | 销售部 | 销售人员 |
| head_nurse1 | head_nurse | 护理部 | 护士长 |
| nurse1 | nurse | 护理部 | 护士 |
| nurse2 | nurse | 护理部 | 护士 |
| doctor1 | doctor | 医疗部 | 医生 |
| doctor2 | doctor | 医疗部 | 医生 |

## 清理内容

脚本会删除以下数据：

- ❌ 所有非初始用户账户
- ❌ 所有预约记录（appointments）
- ❌ 所有排班记录（schedules）
- ❌ 所有任务历史记录（task_history）
- ❌ 与删除用户相关的所有关联数据

## 保留内容

脚本会保留以下数据：

- ✅ 初始用户账户（7 个）
- ✅ 服务配置（services）
- ✅ 资源配置（resources）
- ✅ 部门配置（dingtalk_departments）
- ✅ 系统配置（system_configs）

## 使用方法

### 前置条件

1. 确保 PostgreSQL 数据库正在运行
2. 确保数据库连接配置正确（检查 `.env` 文件）
3. 确保有足够的权限删除数据

### 执行清理

有两个脚本可用：

**方案 1: 智能清理（推荐）**
```bash
node clean-test-data.cjs
```
- 只删除非初始用户创建的数据
- 保留初始用户创建的预约和排班
- 更安全，不会误删重要数据

**方案 2: 彻底清理**
```bash
node clean-all-test-data.cjs
```
- 删除所有预约、排班和非初始用户
- 完全恢复到初始状态
- 适合完全重置数据库

### 使用 npm 脚本（可选）

在 `package.json` 的 scripts 中添加：
```json
"clean:test-data": "node clean-test-data.cjs",
"clean:all-data": "node clean-all-test-data.cjs"
```

然后运行：
```bash
npm run clean:test-data      # 智能清理
npm run clean:all-data       # 彻底清理
```

### 执行流程

1. 脚本会显示清理前的数据统计
2. 显示 5 秒倒计时，期间可按 `Ctrl+C` 取消
3. 开始删除测试数据（按依赖关系顺序）
4. 显示清理后的数据统计
5. 列出保留的初始用户

## 输出示例

### 彻底清理的输出

```
🧹 开始彻底清理所有测试数据，仅保留初始用户...

📊 清理前数据统计:
  • 用户: 16
  • 预约: 138
  • 排班: 104
  • 任务执行: 37
  • 服务: 9
  • 资源: 11

⚠️ 警告：此操作将删除所有测试数据！
� 如果识需要继续，请在 5 秒内按 Ctrl+C 取消...

🔍 正在识别初始用户...
✅ 找到 5 个初始用户

🗑️ 开始删除所有测试数据...

  • 删除任务执行记录...
    ✅ 已删除 37 条任务执行记录

  • 删除排班记录...
    ✅ 已删除 104 条排班记录

  • 删除预约记录...
    ✅ 已删除 138 条预约记录

  • 删除非初始用户账户...
    ✅ 已删除 11 个用户账户

📊 清理后数据统计:
  • 用户: 5
  • 预约: 0
  • 排班: 0
  • 任务执行: 0
  • 服务: 9
  • 资源: 11

✅ 保留的初始用户:
  1. admin (系统管理员) - super_admin
  2. sales1 (张销售) - sales
  3. head_nurse1 (李护士长) - head_nurse
  4. doctor1 (陈医生) - doctor
  5. doctor2 (赵医生) - doctor

🎉 清理完成！数据库已恢复到初始状态

🔌 数据库连接已关闭
```

## 常见问题

### Q: 如何恢复已删除的数据？

A: 建议在执行清理前进行数据库备份。如果需要恢复，可以：
1. 从备份恢复数据库
2. 或者重新运行初始化脚本：`npm run db:init`

### Q: 可以只删除某些用户的数据吗？

A: 可以修改脚本中的 `INITIAL_USERS` 数组来改变保留的用户列表。

### Q: 脚本执行失败怎么办？

A: 检查以下几点：
1. 数据库连接是否正常
2. 用户权限是否足够
3. 数据库中是否存在所有表
4. 查看错误信息中的具体问题

### Q: 清理需要多长时间？

A: 取决于数据量，通常：
- 小数据量（< 10000 条）：几秒钟
- 中等数据量（10000-100000 条）：几十秒
- 大数据量（> 100000 条）：可能需要几分钟

## 安全建议

1. **备份数据**：执行前务必备份数据库
2. **测试环境**：建议先在测试环境验证
3. **权限控制**：确保只有授权人员可以执行此脚本
4. **日志记录**：脚本会输出详细的执行日志，建议保存

## 相关脚本

- `clear-appointments.cjs` - 仅清理预约和排班数据
- `database/migrate.sh` - 数据库迁移脚本
- `scripts/setup-nurse-workflow.cjs` - 护士工作流设置

## 支持

如有问题，请检查：
1. 数据库连接配置
2. 用户权限设置
3. 数据库日志文件
