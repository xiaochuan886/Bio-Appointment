# 钉钉组织架构同步功能说明

## 📋 功能概述

Bio-Appointment 系统现已集成钉钉组织架构同步功能，支持将钉钉企业的部门和用户信息同步到本地系统，实现统一的用户管理。

## ✨ 核心功能

### 1. 钉钉授权与配置

**配置项：**
- **AppKey**：钉钉应用的唯一标识
- **AppSecret**：钉钉应用密钥（加密存储）
- **AgentId**：钉钉应用的 AgentId
- **CorpId**：企业的 CorpId

**配置界面：**
- 位置：用户管理 → 钉钉同步 → 配置按钮
- 所有配置信息存储在数据库中（dingtalk_sync_config 表）
- 支持随时更新配置
- **权限要求**：只有超级管理员（super_admin）可以配置

**配置保存机制：**
- 前端提交配置后，后端会验证用户权限
- 检查用户是否为超级管理员且状态为活跃
- 如果已存在配置则更新，否则创建新配置
- 所有操作都有详细的日志记录

### 2. 同步触发机制

#### 手动同步
- 点击"立即同步"按钮触发
- 实时显示同步进度
- 同步完成后显示详细结果

#### 自动同步（可选）
- 支持设置定时同步计划
- 同步频率选项：
  - 每小时
  - 每天
  - 每周
- 可设置具体同步时间

### 3. 同步范围选择

**部门选择：**
- 自动获取钉钉企业的所有部门
- 支持选择特定部门进行同步
- 默认同步所有部门

**部门映射：**
- 钉钉部门自动映射到本地部门
- 支持手动调整部门映射关系
- 保留部门层级结构

### 4. 数据处理逻辑

#### 用户信息映射

| 钉钉字段 | 本地字段 | 说明 |
|---------|---------|------|
| userid | username | 用户名（唯一标识） |
| name | full_name | 真实姓名 |
| mobile | 用于生成默认密码 | 手机号 |
| email | email | 邮箱地址 |
| dept_id_list | department | 部门信息 |

#### 冲突解决策略

**1. 以钉钉数据为准（默认）**
- 当本地用户与钉钉用户冲突时
- 使用钉钉数据覆盖本地数据
- 适用于钉钉为主要数据源的场景

**2. 保留本地数据**
- 跳过已存在的用户
- 仅同步新增用户
- 适用于本地有重要修改的场景

**3. 手动处理**
- 记录冲突信息
- 不自动处理冲突
- 需要管理员手动审核

#### 增量同步

**优化策略：**
- 首次同步：全量同步所有用户
- 后续同步：仅同步变更的用户
- 减少 API 调用次数
- 提高同步效率

**变更检测：**
- 新增用户：自动创建
- 更新用户：根据策略处理
- 删除用户：标记为禁用（不删除数据）

### 5. 操作反馈与日志

#### 实时进度显示

**同步状态：**
- 🔵 进行中：正在同步
- ✅ 成功：同步完成
- ❌ 失败：同步失败
- ⚠️ 部分成功：部分用户同步失败
- ⏳ 待处理：等待执行

#### 详细日志记录

**日志信息包含：**
- 同步时间（开始和结束）
- 同步类型（手动/自动/增量）
- 总用户数
- 成功数量
- 失败数量
- 跳过数量
- 错误详情
- 操作人

#### 错误处理

**常见错误及解决方案：**

1. **配置错误**
   - 错误：未找到钉钉配置
   - 解决：先配置钉钉应用信息

2. **认证失败**
   - 错误：获取 access_token 失败
   - 解决：检查 AppKey 和 AppSecret 是否正确

3. **权限不足**
   - 错误：无法获取部门或用户列表
   - 解决：确认钉钉应用有通讯录读取权限

4. **用户创建失败**
   - 错误：创建用户失败
   - 解决：检查用户名是否重复，邮箱格式是否正确

## 🎯 使用指南

### 第一步：配置钉钉应用

1. **登录钉钉开发者后台**
   - 访问：https://open-dev.dingtalk.com/
   - 创建企业内部应用

2. **获取应用凭证**
   - AppKey：应用的唯一标识
   - AppSecret：应用密钥
   - AgentId：应用 ID
   - CorpId：企业 ID

3. **配置应用权限**
   - 通讯录管理 → 成员信息读权限
   - 通讯录管理 → 部门信息读权限

4. **在系统中配置**
   - 进入：用户管理 → 钉钉同步
   - 点击"配置"按钮
   - 填写应用凭证
   - 保存配置

### 第二步：首次同步

1. **启用同步功能**
   - 在配置界面开启"启用同步"开关

2. **选择冲突策略**
   - 推荐首次使用"以钉钉数据为准"

3. **执行同步**
   - 点击"立即同步"按钮
   - 等待同步完成
   - 查看同步结果

4. **验证同步结果**
   - 切换到"用户列表"标签
   - 检查同步的用户信息
   - 确认部门映射正确

### 第三步：配置自动同步（可选）

1. **启用自动同步**
   - 在配置界面开启"启用自动同步"开关

2. **设置同步计划**
   - 选择同步频率（每天推荐）
   - 设置同步时间（凌晨 2:00 推荐）

3. **保存配置**
   - 系统将按计划自动执行同步

## 📊 统计信息

**同步面板显示：**
- 总同步次数：历史同步总次数
- 成功次数：成功完成的同步次数
- 失败次数：失败的同步次数
- 已同步用户：累计同步的用户总数

## 🔒 安全说明

### 数据安全

1. **密钥加密存储**
   - AppSecret 加密存储在数据库
   - 仅超级管理员可以查看和修改

2. **权限控制**
   - 仅超级管理员可以配置同步
   - 仅超级管理员可以触发同步
   - 普通用户只能查看同步日志

3. **API 调用安全**
   - 所有钉钉 API 调用在 Edge Function 中执行
   - 不在前端暴露 AppSecret
   - 使用 HTTPS 加密传输

### 数据隐私

1. **最小权限原则**
   - 仅请求必要的通讯录权限
   - 不获取敏感个人信息

2. **数据保留**
   - 同步的用户数据仅用于系统内部
   - 不向第三方共享

## 🛠️ 技术实现

### 架构设计

```
前端 (React)
  ↓
API 层 (src/db/api.ts)
  ↓
Supabase Edge Function (dingtalk-sync)
  ↓
钉钉 API
```

### 数据库表结构

**1. dingtalk_sync_config（同步配置表）**
- 存储钉钉应用配置
- 同步策略设置
- 自动同步计划

**2. dingtalk_sync_logs（同步日志表）**
- 记录每次同步的详细信息
- 包含成功/失败统计
- 错误详情记录

**3. dingtalk_department_mapping（部门映射表）**
- 钉钉部门与本地部门的映射关系
- 支持手动调整映射

### Edge Function

**功能：**
- 获取钉钉 access_token
- 获取部门列表
- 获取用户列表（支持分页）
- 创建/更新本地用户
- 处理冲突
- 记录日志

**优势：**
- 保护 AppSecret 安全
- 服务端执行，性能更好
- 支持长时间运行的同步任务

## 📝 注意事项

### 使用限制

1. **API 调用频率**
   - 钉钉 API 有调用频率限制
   - 建议不要频繁手动同步
   - 自动同步建议设置为每天一次

2. **用户数量**
   - 支持大量用户同步
   - 使用分页获取，避免超时
   - 首次同步可能需要较长时间

3. **网络要求**
   - 需要能访问钉钉 API
   - 确保网络稳定

### 最佳实践

1. **首次同步**
   - 选择业务低峰期执行
   - 使用"以钉钉数据为准"策略
   - 同步完成后验证数据

2. **日常维护**
   - 定期查看同步日志
   - 及时处理同步失败的用户
   - 定期更新部门映射

3. **故障处理**
   - 同步失败时查看错误日志
   - 检查钉钉应用配置
   - 必要时重新配置应用凭证

## 🔧 常见问题排查

### 问题1：配置保存一直显示"保存中..."

**可能原因：**
1. 当前登录用户不是超级管理员
2. 用户状态不是"活跃"
3. 数据库权限配置问题
4. 网络连接问题

**解决方案：**
1. **检查用户角色**
   - 打开浏览器开发者工具（F12）
   - 切换到 Console 标签
   - 查看日志输出，确认用户角色
   - 如果不是 super_admin，请联系管理员修改权限

2. **检查用户状态**
   - 确认用户状态为"活跃"（active）
   - 如果状态为"禁用"，无法保存配置

3. **查看详细错误**
   - 在 Console 中查看完整的错误信息
   - 错误信息会显示具体的失败原因
   - 常见错误：
     - "未登录，请先登录" → 需要重新登录
     - "只有超级管理员可以配置钉钉同步" → 权限不足
     - "获取用户信息失败" → 数据库连接问题

4. **重试步骤**
   - 刷新页面重新登录
   - 确认网络连接正常
   - 清除浏览器缓存后重试

### 问题2：同步失败

**可能原因：**
1. 钉钉应用配置错误
2. 网络无法访问钉钉 API
3. 钉钉应用权限不足

**解决方案：**
1. 验证配置信息是否正确
2. 检查钉钉应用是否已启用
3. 确认应用有通讯录读取权限
4. 查看同步日志中的详细错误信息

### 问题3：部分用户同步失败

**可能原因：**
1. 用户名重复
2. 邮箱格式不正确
3. 必填字段缺失

**解决方案：**
1. 查看同步日志中的失败详情
2. 手动修正有问题的用户信息
3. 重新执行同步

## 🔄 更新日志

### v1.0.1 (2025-11-30)

**问题修复：**
- 🐛 修复配置保存一直显示"保存中..."的问题
- 🐛 增强权限验证逻辑
- 🐛 添加详细的错误日志输出
- 🐛 改进错误提示信息

**优化改进：**
- ✨ 添加用户角色和状态检查
- ✨ 优化配置保存流程
- ✨ 增强错误处理机制
- ✨ 添加详细的操作日志

### v1.0.0 (2025-01-30)

**新增功能：**
- ✅ 钉钉应用配置管理
- ✅ 手动同步功能
- ✅ 自动同步计划
- ✅ 冲突解决策略
- ✅ 部门映射管理
- ✅ 详细同步日志
- ✅ 统计信息展示
- ✅ 实时进度显示

**技术特性：**
- ✅ Edge Function 实现
- ✅ 数据加密存储
- ✅ 权限控制
- ✅ 错误处理
- ✅ 分页获取数据

## 📞 技术支持

如遇到问题，请：

1. 查看同步日志中的错误信息
2. 检查钉钉应用配置是否正确
3. 确认钉钉应用权限是否足够
4. 查看浏览器控制台的详细日志

---

**提示：** 钉钉同步功能需要超级管理员权限才能使用。首次使用前请确保已正确配置钉钉应用信息。
