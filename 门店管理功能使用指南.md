# 门店管理功能使用指南

## 概述

本指南详细介绍了Bio-Appointment系统中的门店管理功能，包括门店的创建、管理、权限控制以及与其他模块的集成使用方法。

## 目录

1. [门店管理概述](#门店管理概述)
2. [门店CRUD操作](#门店crud操作)
3. [门店与用户、房间、预约的关联](#门店与用户房间预约的关联)
4. [权限控制](#权限控制)
5. [预约流程中的门店选择](#预约流程中的门店选择)
6. [排班功能的门店范围限制](#排班功能的门店范围限制)
7. [常见问题与解决方案](#常见问题与解决方案)
8. [测试验证方法](#测试验证方法)

## 门店管理概述

### 功能简介

门店管理是Bio-Appointment系统的核心功能之一，它实现了多门店运营支持，包括：

- 门店信息的创建、编辑、删除
- 门店状态管理（营业中/已停业）
- 门店与用户、资源、预约的关联
- 基于门店的权限控制
- 门店营业时间设置

### 数据结构

门店数据包含以下主要字段：

```typescript
interface Store {
  id: string;                    // 门店唯一标识
  name: string;                  // 门店名称
  address?: string;               // 门店地址
  phone?: string;                 // 门店联系电话
  contact_person?: string;        // 门店联系人
  status: 'active' | 'inactive'; // 门店状态
  description?: string;           // 门店描述
  business_hours?: Record<string, any>; // 营业时间
  created_at: string;            // 创建时间
  updated_at: string;            // 更新时间
  created_by?: string;            // 创建人
  updated_by?: string;            // 更新人
}
```

## 门店CRUD操作

### 创建门店

**权限要求**：仅管理员（super_admin）可创建门店

**操作步骤**：
1. 登录管理员账户
2. 导航到"门店管理"页面
3. 点击"添加门店"按钮
4. 填写门店信息：
   - 门店名称（必填）
   - 门店地址（必填）
   - 联系电话（必填）
   - 联系人（必填）
   - 门店状态（默认为"营业中"）
   - 门店描述（可选）
   - 营业时间（可选）
5. 点击"保存"按钮

**API调用示例**：
```javascript
const storeData = {
  name: '北京朝阳门店',
  address: '北京市朝阳区建国路88号',
  phone: '400-123-4567',
  contact_person: '张经理',
  status: 'active',
  description: '朝阳地区主要服务门店',
  business_hours: {
    monday: { open: '09:00', close: '18:00' },
    tuesday: { open: '09:00', close: '18:00' },
    // ... 其他天
  }
};

const response = await fetch('/api/stores', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${adminToken}`
  },
  body: JSON.stringify(storeData)
});
```

### 查看门店列表

**权限要求**：所有登录用户可查看门店列表

**操作步骤**：
1. 登录系统
2. 导航到"门店管理"页面
3. 查看门店列表，包含：
   - 门店名称
   - 地址
   - 联系电话
   - 联系人
   - 状态
   - 创建时间
   - 操作按钮（查看、编辑、删除）

**筛选功能**：
- 按门店名称搜索
- 按状态筛选（全部/营业中/已停业）

### 更新门店信息

**权限要求**：仅管理员（super_admin）可更新门店

**操作步骤**：
1. 在门店列表中找到要更新的门店
2. 点击"编辑"按钮
3. 修改需要更新的信息
4. 点击"保存"按钮

### 删除门店

**权限要求**：仅管理员（super_admin）可删除门店

**注意事项**：
- 删除门店前需要确保没有关联的预约、用户或资源
- 如果有关联数据，系统会阻止删除操作

**操作步骤**：
1. 在门店列表中找到要删除的门店
2. 点击"删除"按钮
3. 确认删除操作

## 门店与用户、房间、预约的关联

### 用户与门店关联

**关联方式**：
1. 管理员在创建或编辑用户时指定门店
2. 用户登录后自动关联到指定门店

**关联的用户类型**：
- 护士（nurse）
- 医生（doctor）
- 护士长（head_nurse）

**API调用示例**：
```javascript
const userData = {
  username: 'nurse_beijing',
  password: '123456',
  full_name: '北京护士',
  role: 'nurse',
  store_id: '门店ID' // 关联到指定门店
};
```

### 房间与门店关联

**关联方式**：
1. 管理员在创建或编辑房间时指定门店
2. 房间自动归属于指定门店

**API调用示例**：
```javascript
const roomData = {
  name: 'VIP采血室1',
  type: 'vip',
  is_available: true,
  store_id: '门店ID' // 关联到指定门店
};
```

### 预约与门店关联

**关联方式**：
1. 销售在创建预约时选择门店
2. 预约自动关联到指定门店

**API调用示例**：
```javascript
const appointmentData = {
  customer_name: '张三',
  customer_phone: '13800138000',
  service_id: '服务ID',
  requested_date: '2025-12-10',
  requested_time_start: '10:00:00',
  requested_time_end: '11:00:00',
  store_id: '门店ID' // 关联到指定门店
};
```

## 权限控制

### 角色权限矩阵

| 功能 | 管理员 | 销售 | 护士长 | 医生 | 护士 |
|------|--------|------|--------|------|------|
| 查看所有门店 | ✅ | ✅ | ❌ | ❌ | ❌ |
| 创建门店 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 更新门店 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 删除门店 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 查看自己门店 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 创建跨门店预约 | ✅ | ✅ | ❌ | ❌ | ❌ |
| 查看跨门店排班 | ✅ | ❌ | ❌ | ❌ | ❌ |

### 权限验证规则

1. **门店访问控制**：
   - 管理员和销售可以访问所有门店
   - 其他角色只能访问自己所属的门店

2. **数据隔离**：
   - 护士长只能看到自己门店的排班
   - 医生只能看到自己相关的预约
   - 护士只能看到自己分配的任务

3. **操作权限**：
   - 门店的增删改仅限管理员
   - 预约创建销售可以跨门店
   - 排班管理仅限自己门店

## 预约流程中的门店选择

### 门店选择界面

**位置**：销售预约页面

**功能特点**：
- 只显示状态为"营业中"的门店
- 显示门店名称和地址信息
- 支持门店搜索和筛选

### 门店选择流程

1. 销售登录系统
2. 导航到"预约发起"页面
3. 在"选择门店"下拉框中选择目标门店
4. 系统显示门店地址信息
5. 继续填写其他预约信息

### 门店资源可用性检查

选择门店后，系统会：
1. 检查该门店的房间可用性
2. 检查该门店的护士可用性
3. 根据检查结果显示可用时间段

### API调用示例

```javascript
// 获取活跃门店列表
const stores = await fetch('/api/stores?status=active', {
  headers: {
    'Authorization': `Bearer ${salesToken}`
  }
});

// 创建带门店的预约
const appointmentData = {
  customer_name: '李四',
  service_id: 'service-id',
  requested_date: '2025-12-10',
  store_id: 'selected-store-id', // 选中的门店
  // ... 其他字段
};
```

## 排班功能的门店范围限制

### 护士长排班权限

**权限范围**：
- 护士长只能为自己所属的门店创建排班
- 不能查看或操作其他门店的排班
- 只能分配自己门店的护士和房间

### 排班界面限制

1. **门店筛选**：
   - 护士长登录后自动筛选自己门店的预约
   - 不显示其他门店的预约

2. **资源选择**：
   - 房间列表只显示自己门店的房间
   - 护士列表只显示自己门店的护士

3. **预约列表**：
   - 只显示自己门店的预约
   - 跨门店预约不可见

### API调用示例

```javascript
// 护士长获取自己门店的排班
const schedules = await fetch('/api/schedules?store_id=nurse-store-id', {
  headers: {
    'Authorization': `Bearer ${nurseManagerToken}`
  }
});

// 尝试访问其他门店排班（会被拒绝）
const otherSchedules = await fetch('/api/schedules?store_id=other-store-id', {
  headers: {
    'Authorization': `Bearer ${nurseManagerToken}`
  }
});
// 返回：403 Forbidden
```

## 常见问题与解决方案

### 问题1：门店创建失败

**可能原因**：
- 缺少必填字段
- 门店名称重复
- 权限不足

**解决方案**：
1. 检查所有必填字段是否填写完整
2. 确保门店名称唯一
3. 确认使用管理员账户登录

### 问题2：无法看到某些门店

**可能原因**：
- 门店状态为"已停业"
- 权限限制
- 数据同步延迟

**解决方案**：
1. 检查门店状态是否为"营业中"
2. 确认账户角色和权限
3. 刷新页面或重新登录

### 问题3：预约时门店选择为空

**可能原因**：
- 没有活跃状态的门店
- 权限问题
- API连接问题

**解决方案**：
1. 确保有至少一个门店状态为"营业中"
2. 检查销售账户权限
3. 检查网络连接和API状态

### 问题4：护士长无法查看排班

**可能原因**：
- 护士长未关联到门店
- 门店关联错误
- 权限配置问题

**解决方案**：
1. 检查护士长账户的门店关联
2. 确认门店ID正确
3. 联系管理员检查权限配置

### 问题5：跨门店数据泄露

**可能原因**：
- 权限验证逻辑错误
- API端点未正确实现权限检查
- 数据库查询未过滤门店

**解决方案**：
1. 检查所有API端点的权限验证逻辑
2. 确保数据库查询包含门店过滤条件
3. 进行全面的安全测试

## 测试验证方法

### 自动化测试

使用提供的测试脚本进行自动化验证：

```bash
# 运行完整测试套件
node test-store-management-comprehensive.cjs

# 运行模拟测试（绕过认证）
node test-store-management-mock.cjs
```

### 手动测试清单

#### 门店CRUD测试
- [ ] 管理员可以创建门店
- [ ] 管理员可以查看门店列表
- [ ] 管理员可以更新门店信息
- [ ] 管理员可以删除门店
- [ ] 非管理员无法创建门店

#### 门店关联测试
- [ ] 用户可以关联到门店
- [ ] 房间可以关联到门店
- [ ] 预约可以关联到门店
- [ ] 按门店过滤数据正确

#### 权限控制测试
- [ ] 管理员可以访问所有门店
- [ ] 销售可以访问所有门店（为创建预约）
- [ ] 护士长只能访问自己门店
- [ ] 医生只能访问相关数据
- [ ] 护士只能访问分配的任务

#### 预约流程测试
- [ ] 销售可以看到活跃门店列表
- [ ] 销售可以为任意门店创建预约
- [ ] 预约正确关联到门店
- [ ] 门店资源可用性检查正常

#### 排班限制测试
- [ ] 护士长只能看到自己门店的预约
- [ ] 护士长只能分配自己门店的资源
- [ ] 护士长无法访问其他门店排班
- [ ] 排班数据正确关联到门店

### 测试报告分析

测试完成后，查看生成的测试报告文件：
- `store-management-test-report.json`
- `store-management-test-report-mock.json`

报告包含：
- 测试执行摘要
- 分类测试结果
- 发现的问题列表
- 改进建议

## 最佳实践

### 门店管理
1. 使用描述性的门店名称
2. 保持门店信息准确性
3. 及时更新门店状态
4. 定期审查门店关联数据

### 权限管理
1. 遵循最小权限原则
2. 定期审查用户权限
3. 及时更新用户门店关联
4. 监控跨门店访问行为

### 数据安全
1. 实施严格的数据隔离
2. 定期进行安全审计
3. 记录所有门店访问日志
4. 建立异常访问告警机制

## 总结

门店管理功能是Bio-Appointment系统实现多门店运营的核心组件，通过合理的权限控制和数据关联，确保了系统的安全性和可用性。正确使用本指南中的功能和方法，可以有效管理多门店环境下的预约、排班和资源分配。

如有问题或需要进一步的技术支持，请参考系统文档或联系技术团队。