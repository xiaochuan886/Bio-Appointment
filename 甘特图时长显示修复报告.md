# 甘特图时长显示修复报告

## 问题描述

在甘特图组件中,当排班被编辑并调整了时长后,甘特图条的长度和结束时间没有更新,仍然显示预约时的原始时长。只有在弹窗中的信息才是正确的(显示调整后的时长)。

### 问题表现
- **弹窗信息**: 正确显示 15:00-16:30, 时长90分钟(已调整)
- **甘特图条**: 错误显示原始预约时长,未反映调整后的时长

## 问题根源

`getSchedulePosition` 函数直接使用 `scheduled_time_end` 来计算甘特图条的宽度:

```typescript
const getSchedulePosition = (timeStart: string, timeEnd: string) => {
  const [startHour, startMinute] = timeStart.split(':').map(Number);
  const [endHour, endMinute] = timeEnd.split(':').map(Number);
  
  const startMinutes = (startHour - 8) * 60 + startMinute;
  const endMinutes = (endHour - 8) * 60 + endMinute;
  const duration = endMinutes - startMinutes;
  // ...
};
```

当排班被编辑并调整时长后:
- `adjusted_duration` 字段被更新为新的时长
- 但 `scheduled_time_end` 没有相应更新
- 导致甘特图仍然使用旧的结束时间计算宽度

## 解决方案

修改 `getSchedulePosition` 函数,优先使用 `adjusted_duration` 来计算结束时间:

```typescript
const getSchedulePosition = (timeStart: string, timeEnd: string, adjustedDuration?: number) => {
  const [startHour, startMinute] = timeStart.split(':').map(Number);
  const startMinutes = (startHour - 8) * 60 + startMinute;
  
  let endMinutes: number;
  
  // 如果有调整后的时长,使用它来计算结束时间
  if (adjustedDuration !== undefined && adjustedDuration !== null) {
    endMinutes = startMinutes + adjustedDuration;
  } else {
    // 否则使用原始的结束时间
    const [endHour, endMinute] = timeEnd.split(':').map(Number);
    endMinutes = (endHour - 8) * 60 + endMinute;
  }
  
  const duration = endMinutes - startMinutes;
  
  const left = (startMinutes / (11 * 60)) * 100;
  const width = (duration / (11 * 60)) * 100;
  
  return { left: `${left}%`, width: `${width}%` };
};
```

## 修改文件

### 1. `src/components/appointment/GanttChart.tsx`
- 修改 `getSchedulePosition` 函数签名,添加 `adjustedDuration` 参数
- 更新所有调用 `getSchedulePosition` 的地方,传入 `schedule.adjusted_duration`
- 影响范围:
  - 日视图 - 房间排班
  - 日视图 - 护士排班

### 2. `src/components/dashboard/ReadOnlyGanttChart.tsx`
- 修改 `getSchedulePosition` 函数签名,添加 `adjustedDuration` 参数
- 更新所有调用 `getSchedulePosition` 的地方,传入 `schedule.adjusted_duration`
- 影响范围:
  - 日视图 - 房间排班
  - 日视图 - 护士排班
  - 日视图 - 医生排班

## 修复效果

修复后,甘特图将正确显示:
1. **未调整的排班**: 使用原始的 `scheduled_time_end` 计算长度
2. **已调整的排班**: 使用 `adjusted_duration` 计算长度,确保甘特图条的长度与实际时长一致

### 示例
- 原始预约: 15:00-16:00 (60分钟)
- 编辑后: 15:00-16:30 (90分钟, `adjusted_duration = 90`)
- 甘特图显示: 从15:00开始,宽度为90分钟(正确到16:30)

## 测试建议

1. 创建一个排班
2. 在甘特图中查看排班条的长度
3. 编辑排班,调整时长
4. 刷新页面,确认甘特图条的长度已更新
5. 验证弹窗和甘特图显示的时长一致

## 技术细节

### 数据流
```
排班编辑 → adjusted_duration 更新 → 数据库保存
                                    ↓
                            前端重新加载排班数据
                                    ↓
                    getSchedulePosition 使用 adjusted_duration
                                    ↓
                            甘特图条长度正确显示
```

### 兼容性
- 向后兼容: 如果 `adjusted_duration` 为空,仍使用原始的 `scheduled_time_end`
- 不影响未调整的排班显示
- 只影响已调整时长的排班显示

## 修复日期
2024-12-16

## 相关问题
- 排班详情弹窗显示正确(已有 `adjusted_duration` 支持)
- Tooltip 悬停提示显示正确(已有 `adjusted_duration` 支持)
- 只有甘特图条的视觉长度显示不正确(已修复)
