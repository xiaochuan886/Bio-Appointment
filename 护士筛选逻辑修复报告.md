# 护士筛选逻辑修复报告

## 问题描述

护士的"我的任务"、"我的排班"、"任务历史"页面的筛选条件不正确，当前使用了门店权限筛选，但护士可能临时支援其他门店，导致护士看不到分配给自己但不在默认门店的任务。

## 问题分析

### 原有逻辑问题

1. **TaskPage.tsx (我的任务)**
   - 使用 `getAccessibleStoreIds()` 进行门店筛选
   - 护士临时支援其他门店的任务会被过滤掉

2. **HistoryPage.tsx (任务历史)**
   - 同样使用门店权限筛选
   - 护士看不到在其他门店执行的历史任务

3. **SchedulePage.tsx (我的排班)**
   - 使用门店权限筛选
   - 护士看不到临时支援其他门店的排班

### 服务端API状态

服务端API (`server/api-server.cjs`) 已经正确实现：
- 护士角色：`s.nurse_id = userProfile.id` (只查看自己的排班)
- 前端额外的门店筛选是多余的

## 修复方案

### 核心原则

**护士的个人页面应该只筛选分配给护士本人的数据，不考虑门店限制**

### 具体修改

#### 1. TaskPage.tsx 修改

```typescript
// 修改前
const storeFilter = getAccessibleStoreIds(user?.profile || null);
const schedulesData = await clientApi.getSchedules({
  date: today,
  store_id: storeFilter || undefined
});

// 修改后  
const schedulesData = await clientApi.getSchedules({
  date: today,
  nurse_id: user?.profile?.id // 只筛选分配给当前护士的任务
});
```

#### 2. HistoryPage.tsx 修改

```typescript
// 修改前
const storeFilter = getAccessibleStoreIds(user?.profile || null);
const params: any = {
  start_date: format(dateRange.start, 'yyyy-MM-dd'),
  end_date: format(dateRange.end, 'yyyy-MM-dd'),
  store_id: storeFilter || undefined
};

// 修改后
const params: any = {
  start_date: format(dateRange.start, 'yyyy-MM-dd'),
  end_date: format(dateRange.end, 'yyyy-MM-dd')
};
// nurse_id 在后面添加
```

#### 3. SchedulePage.tsx 修改

```typescript
// 修改前
const storeFilter = getAccessibleStoreIds(user?.profile || null);
let params: any = {
  store_id: storeFilter || undefined
};

// 修改后
let params: any = {};
// nurse_id 在后面根据用户信息添加
```

#### 4. 简化数据验证逻辑

所有页面的数据验证都简化为：

```typescript
// 修改前
const validTasks = allTasks.filter(task => {
  const userProfile = user?.profile || null;
  const taskStoreId = task.store_id || task.appointment?.store_id;
  if (!canManageStoreSchedule(userProfile, taskStoreId)) {
    return false;
  }
  // ... 其他验证
});

// 修改后
const validTasks = allTasks.filter(task => {
  // 服务端API已经根据nurse_id筛选了数据，这里只需要基本验证
  if (!task.appointment_id) {
    console.warn('任务缺少关联预约:', task.id);
    return false;
  }
  return true;
});
```

## 修复效果

### 修复前
- ❌ 护士只能看到默认门店的任务
- ❌ 临时支援其他门店的任务不可见
- ❌ 历史记录不完整

### 修复后
- ✅ 护士可以看到分配给自己的所有任务
- ✅ 包括临时支援其他门店的任务
- ✅ 历史记录完整
- ✅ 只显示分配给护士本人的数据

## 测试验证

创建了测试脚本 `test-nurse-filter-fix.cjs` 来验证修复效果：

```bash
node test-nurse-filter-fix.cjs
```

测试场景：
1. 创建护士在不同门店的排班
2. 验证API返回的数据
3. 检查跨门店支援任务的可见性
4. 确认只显示分配给护士本人的任务

## 影响范围

### 受影响的文件
- `src/pages/nurse/TaskPage.tsx`
- `src/pages/nurse/HistoryPage.tsx` 
- `src/pages/nurse/SchedulePage.tsx`

### 不受影响的功能
- 管理员和护士长的权限控制保持不变
- 门店权限系统继续正常工作
- 其他角色的页面不受影响

## 注意事项

1. **权限边界清晰**：护士个人页面不使用门店权限，管理页面继续使用门店权限
2. **数据安全**：服务端API已有正确的权限控制，前端只是移除了多余的筛选
3. **向后兼容**：修改不影响现有的权限系统架构

## 总结

此次修复解决了护士无法看到临时支援其他门店任务的问题，使护士的个人页面能够正确显示所有分配给自己的任务，无论这些任务在哪个门店执行。修复遵循了"护士个人数据不受门店限制"的原则，提升了用户体验。