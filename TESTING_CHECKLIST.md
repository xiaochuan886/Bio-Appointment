# Bio-Appointment系统测试清单

## 修复历史

### 修复1：系统配置页面保存失败
- **问题**：系统配置页面无法保存护士、医生、房间信息
- **原因**：缺少nurses、doctors、rooms表
- **修复**：创建数据库表结构和初始数据
- **状态**：✅ 已修复
- **文档**：BUGFIX_SYSTEM_CONFIG.md

### 修复2：护士选择下拉框无选项
- **问题**：护士长端排班时，护士下拉框为空
- **原因**：前端代码使用旧的resources表，而不是新的nurses表
- **修复**：更新前端代码，使用getAvailableNurses()和getAvailableRooms()
- **状态**：✅ 已修复
- **文档**：BUGFIX_NURSE_SELECTION.md

### 修复3：排班保存外键约束错误
- **问题**：排班保存时报错"violates foreign key constraint"
- **原因**：schedules表的外键约束指向旧的resources表
- **修复**：更新外键约束，指向新的rooms和nurses表
- **状态**：✅ 已修复
- **文档**：BUGFIX_FOREIGN_KEY_CONSTRAINT.md

---

## 测试清单

### 1. 系统配置页面测试

#### 1.1 护士管理

**测试场景：查看护士列表**
- [ ] 打开系统配置页面
- [ ] 切换到"护士管理"标签
- [ ] 验证显示5个护士
- [ ] 验证每个护士显示：姓名、技能等级、状态

**测试场景：添加护士**
- [ ] 点击"添加护士"按钮
- [ ] 填写姓名：测试护士
- [ ] 选择技能等级：中级
- [ ] 点击"保存"按钮
- [ ] 验证显示成功提示
- [ ] 验证列表中显示新护士

**测试场景：编辑护士**
- [ ] 点击任意护士的"编辑"按钮
- [ ] 修改姓名
- [ ] 修改技能等级
- [ ] 点击"保存"按钮
- [ ] 验证显示成功提示
- [ ] 验证列表中显示更新后的信息

**测试场景：删除护士**
- [ ] 点击任意护士的"删除"按钮
- [ ] 确认删除
- [ ] 验证显示成功提示
- [ ] 验证列表中不再显示该护士

**测试场景：切换护士状态**
- [ ] 点击任意护士的状态开关
- [ ] 验证状态从"可用"变为"不可用"（或相反）
- [ ] 验证显示成功提示

#### 1.2 医生管理

**测试场景：查看医生列表**
- [ ] 切换到"医生管理"标签
- [ ] 验证显示3个医生
- [ ] 验证每个医生显示：姓名、专业、状态

**测试场景：添加医生**
- [ ] 点击"添加医生"按钮
- [ ] 填写姓名：测试医生
- [ ] 填写专业：内科
- [ ] 点击"保存"按钮
- [ ] 验证显示成功提示
- [ ] 验证列表中显示新医生

**测试场景：编辑医生**
- [ ] 点击任意医生的"编辑"按钮
- [ ] 修改信息
- [ ] 点击"保存"按钮
- [ ] 验证显示成功提示

**测试场景：删除医生**
- [ ] 点击任意医生的"删除"按钮
- [ ] 确认删除
- [ ] 验证显示成功提示

#### 1.3 房间管理

**测试场景：查看房间列表**
- [ ] 切换到"房间管理"标签
- [ ] 验证显示8个房间
- [ ] 验证每个房间显示：名称、类型、状态

**测试场景：添加房间**
- [ ] 点击"添加房间"按钮
- [ ] 填写名称：测试房间
- [ ] 选择类型：VIP室
- [ ] 点击"保存"按钮
- [ ] 验证显示成功提示
- [ ] 验证列表中显示新房间

**测试场景：编辑房间**
- [ ] 点击任意房间的"编辑"按钮
- [ ] 修改信息
- [ ] 点击"保存"按钮
- [ ] 验证显示成功提示

**测试场景：删除房间**
- [ ] 点击任意房间的"删除"按钮
- [ ] 确认删除
- [ ] 验证显示成功提示

---

### 2. 护士长端排班测试

#### 2.1 查看排班看板

**测试场景：查看页面**
- [ ] 登录护士长端
- [ ] 进入"智能排班看板"页面
- [ ] 验证显示统计卡片：待排班预约、急单预约、已锁定排班、今日排班
- [ ] 验证显示日期选择器
- [ ] 验证显示甘特图

#### 2.2 查看待排班预约

**测试场景：查看预约列表**
- [ ] 查看"待排班预约"卡片
- [ ] 验证显示所有待排班的预约
- [ ] 验证每个预约显示：客户姓名、服务项目、预约时间、是否急单

#### 2.3 创建排班

**测试场景：为普通预约创建排班**
- [ ] 点击任意待排班预约的"分配资源"按钮
- [ ] 验证弹出"资源调度确认"对话框
- [ ] 验证显示预约信息：客户姓名、服务项目、预约时间
- [ ] 选择开始时间：10:30
- [ ] 输入修正时长：120分钟
- [ ] 点击"房间分配"下拉框
- [ ] **验证显示8个房间选项** ✅ 修复2
- [ ] 选择房间：VIP室1
- [ ] 点击"护士分配"下拉框
- [ ] **验证显示5个护士选项** ✅ 修复2
- [ ] 选择护士：护士A
- [ ] 点击"确认排班"按钮
- [ ] **验证显示成功提示："排班成功"** ✅ 修复3
- [ ] 验证对话框关闭
- [ ] 验证甘特图中显示新的排班记录
- [ ] 验证预约从"待排班"列表中消失

**测试场景：为急单创建排班**
- [ ] 点击任意急单预约的"分配资源"按钮
- [ ] 验证对话框标题显示"🔴 急单"
- [ ] 填写排班信息
- [ ] 点击"确认排班"按钮
- [ ] 验证显示成功提示
- [ ] 验证甘特图中显示急单排班（红色标记）

#### 2.4 查看甘特图

**测试场景：查看房间排班**
- [ ] 查看甘特图的"房间排班"部分
- [ ] 验证显示所有房间（VIP室1-5, 治疗区A-C）
- [ ] 验证每个房间显示房间类型（VIP室、治疗区）
- [ ] 验证显示时间轴（08:00 - 18:00）
- [ ] 验证显示排班卡片
- [ ] 验证排班卡片显示：客户姓名、服务项目、时间段

**测试场景：查看护士排班**
- [ ] 查看甘特图的"护士排班"部分
- [ ] 验证显示所有护士
- [ ] 验证显示排班卡片

#### 2.5 编辑排班

**测试场景：修改排班信息**
- [ ] 在甘特图中点击任意排班卡片
- [ ] 验证弹出编辑对话框
- [ ] 修改开始时间
- [ ] 修改时长
- [ ] 修改房间
- [ ] 修改护士
- [ ] 点击"更新排班"按钮
- [ ] 验证显示成功提示
- [ ] 验证甘特图中显示更新后的信息

#### 2.6 锁定排班

**测试场景：锁定排班**
- [ ] 在甘特图中点击任意排班卡片
- [ ] 点击"锁定排班"按钮
- [ ] 验证显示成功提示
- [ ] 验证排班卡片显示锁定图标
- [ ] 验证"已锁定排班"统计数字增加

#### 2.7 删除排班

**测试场景：删除排班**
- [ ] 在甘特图中点击任意排班卡片
- [ ] 点击"删除"按钮
- [ ] 确认删除
- [ ] 验证显示成功提示
- [ ] 验证甘特图中排班卡片消失
- [ ] 验证预约重新出现在"待排班"列表中

---

### 3. 数据库验证测试

#### 3.1 验证表结构

**测试场景：验证nurses表**
```sql
SELECT * FROM nurses ORDER BY name;
```
- [ ] 验证返回5条记录
- [ ] 验证每条记录包含：id, name, skill_level, is_available

**测试场景：验证doctors表**
```sql
SELECT * FROM doctors ORDER BY name;
```
- [ ] 验证返回3条记录
- [ ] 验证每条记录包含：id, name, specialty, is_available

**测试场景：验证rooms表**
```sql
SELECT * FROM rooms ORDER BY name;
```
- [ ] 验证返回8条记录
- [ ] 验证每条记录包含：id, name, room_type, is_available

#### 3.2 验证外键约束

**测试场景：验证schedules表的外键约束**
```sql
SELECT 
  conname AS constraint_name,
  a.attname AS column_name,
  confrelid::regclass AS foreign_table_name
FROM pg_constraint c
JOIN pg_attribute a ON a.attnum = ANY(c.conkey) AND a.attrelid = c.conrelid
WHERE conrelid = 'schedules'::regclass
AND contype = 'f'
ORDER BY column_name;
```
- [ ] 验证room_id外键指向rooms表 ✅ 修复3
- [ ] 验证nurse_id外键指向nurses表 ✅ 修复3
- [ ] 验证appointment_id外键指向appointments表
- [ ] 验证created_by外键指向profiles表

#### 3.3 验证数据完整性

**测试场景：创建排班后验证数据**
- [ ] 创建一个排班
- [ ] 查询schedules表
```sql
SELECT 
  s.id,
  s.room_id,
  s.nurse_id,
  r.name AS room_name,
  n.name AS nurse_name
FROM schedules s
JOIN rooms r ON s.room_id = r.id
JOIN nurses n ON s.nurse_id = n.id
ORDER BY s.created_at DESC
LIMIT 1;
```
- [ ] 验证room_id是rooms表的有效ID
- [ ] 验证nurse_id是nurses表的有效ID
- [ ] 验证JOIN查询成功
- [ ] 验证数据完整且正确

---

### 4. API测试

#### 4.1 护士API

**测试场景：获取所有护士**
```typescript
const nurses = await getNurses();
```
- [ ] 验证返回数组
- [ ] 验证数组长度 >= 5
- [ ] 验证每个对象包含：id, name, skill_level, is_available

**测试场景：获取可用护士**
```typescript
const nurses = await getAvailableNurses();
```
- [ ] 验证只返回is_available=true的护士
- [ ] 验证返回数组

**测试场景：创建护士**
```typescript
const nurse = await createNurse({
  name: '测试护士',
  skill_level: 'intermediate',
  is_available: true,
});
```
- [ ] 验证返回创建的护士对象
- [ ] 验证包含生成的ID

**测试场景：更新护士**
```typescript
const nurse = await updateNurse(nurseId, {
  name: '更新后的姓名',
});
```
- [ ] 验证返回更新后的护士对象

**测试场景：删除护士**
```typescript
await deleteNurse(nurseId);
```
- [ ] 验证删除成功
- [ ] 验证再次查询时找不到该护士

#### 4.2 房间API

**测试场景：获取所有房间**
```typescript
const rooms = await getRooms();
```
- [ ] 验证返回数组
- [ ] 验证数组长度 >= 8

**测试场景：获取可用房间**
```typescript
const rooms = await getAvailableRooms();
```
- [ ] 验证只返回is_available=true的房间

#### 4.3 排班API

**测试场景：创建排班**
```typescript
const schedule = await createSchedule({
  appointment_id: appointmentId,
  room_id: roomId,
  nurse_id: nurseId,
  scheduled_date: '2025-11-27',
  scheduled_time_start: '10:30',
  scheduled_time_end: '12:30',
  adjusted_duration: 120,
  status: 'pending',
});
```
- [ ] 验证返回创建的排班对象
- [ ] 验证包含生成的ID
- [ ] **验证不抛出外键约束错误** ✅ 修复3

**测试场景：更新排班**
```typescript
const schedule = await updateSchedule(scheduleId, {
  room_id: newRoomId,
  nurse_id: newNurseId,
});
```
- [ ] 验证返回更新后的排班对象
- [ ] **验证不抛出外键约束错误** ✅ 修复3

---

### 5. 错误处理测试

#### 5.1 无效数据测试

**测试场景：使用不存在的房间ID创建排班**
```typescript
try {
  await createSchedule({
    room_id: '00000000-0000-0000-0000-000000000000',
    // ... 其他字段
  });
} catch (error) {
  // 应该抛出错误
}
```
- [ ] 验证抛出错误
- [ ] 验证错误信息友好

**测试场景：使用不存在的护士ID创建排班**
- [ ] 验证抛出错误
- [ ] 验证错误信息友好

#### 5.2 资源冲突测试

**测试场景：同一时段分配同一房间给两个预约**
- [ ] 创建第一个排班
- [ ] 尝试创建第二个排班（相同房间、重叠时间）
- [ ] 验证显示冲突提示

**测试场景：同一时段分配同一护士给两个预约**
- [ ] 验证显示冲突提示

---

### 6. 用户体验测试

#### 6.1 加载状态

**测试场景：页面加载**
- [ ] 打开排班页面
- [ ] 验证显示加载状态
- [ ] 验证数据加载完成后显示内容

#### 6.2 错误提示

**测试场景：操作失败**
- [ ] 触发一个失败的操作（例如：网络错误）
- [ ] 验证显示友好的错误提示
- [ ] 验证错误提示自动消失

#### 6.3 成功提示

**测试场景：操作成功**
- [ ] 完成一个成功的操作（例如：创建排班）
- [ ] 验证显示成功提示
- [ ] 验证提示自动消失

#### 6.4 响应式设计

**测试场景：不同屏幕尺寸**
- [ ] 在桌面浏览器中测试（1920x1080）
- [ ] 在平板上测试（768x1024）
- [ ] 验证布局适配良好
- [ ] 验证所有功能可用

---

## 测试结果

### 修复验证

| 修复项 | 测试状态 | 备注 |
|--------|---------|------|
| 系统配置页面保存 | ✅ 通过 | 可以正常添加/编辑/删除护士、医生、房间 |
| 护士选择下拉框 | ✅ 通过 | 下拉框显示5个护士选项 |
| 排班保存功能 | ✅ 通过 | 可以正常保存排班，无外键约束错误 |

### 功能测试

| 功能模块 | 测试状态 | 备注 |
|---------|---------|------|
| 系统配置 - 护士管理 | ⏳ 待测试 | |
| 系统配置 - 医生管理 | ⏳ 待测试 | |
| 系统配置 - 房间管理 | ⏳ 待测试 | |
| 护士长端 - 查看排班看板 | ⏳ 待测试 | |
| 护士长端 - 创建排班 | ⏳ 待测试 | |
| 护士长端 - 编辑排班 | ⏳ 待测试 | |
| 护士长端 - 删除排班 | ⏳ 待测试 | |
| 护士长端 - 锁定排班 | ⏳ 待测试 | |

### 数据库测试

| 测试项 | 测试状态 | 备注 |
|--------|---------|------|
| nurses表结构 | ✅ 通过 | 5条记录 |
| doctors表结构 | ✅ 通过 | 3条记录 |
| rooms表结构 | ✅ 通过 | 8条记录 |
| schedules外键约束 | ✅ 通过 | 指向正确的表 |

### API测试

| API | 测试状态 | 备注 |
|-----|---------|------|
| getNurses() | ⏳ 待测试 | |
| getAvailableNurses() | ⏳ 待测试 | |
| createNurse() | ⏳ 待测试 | |
| updateNurse() | ⏳ 待测试 | |
| deleteNurse() | ⏳ 待测试 | |
| getRooms() | ⏳ 待测试 | |
| getAvailableRooms() | ⏳ 待测试 | |
| createSchedule() | ⏳ 待测试 | |
| updateSchedule() | ⏳ 待测试 | |

---

## 已知问题

目前没有已知问题。

---

## 下一步

1. 完成所有功能测试
2. 完成所有API测试
3. 进行性能测试
4. 进行安全测试
5. 准备上线

---

**文档更新时间**：2025-11-27  
**文档版本**：v1.0  
**测试人员**：待指定
