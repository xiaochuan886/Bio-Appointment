# 护士长排班页面浏览器测试指南

## 测试环境
- **URL**: http://127.0.0.1:5176/head-nurse/schedule
- **测试时间**: 2025-12-02

## 测试目标
1. 测试三种视图模式（日、周、月）
2. 验证排班数据正确显示
3. 检查客户姓名和服务名称显示
4. 查找JavaScript错误

## 测试步骤

### 1. 页面访问测试
1. 打开浏览器
2. 访问: http://127.0.0.1:5176/head-nurse/schedule
3. 检查页面是否正常加载

### 2. 视图模式测试

#### 日视图测试
1. 点击"日视图"按钮
2. 验证显示当前日期的房间排班时间轴（08:00-18:00）
3. 检查是否显示：
   - 房间名称和类型
   - 护士姓名
   - 排班卡片显示客户姓名和服务名称
   - 时间轴正确显示

#### 周视图测试
1. 点击"周视图"按钮
2. 验证显示本周（周一到周日）的排班汇总
3. 检查是否显示：
   - 每个房间每天的排班数量
   - 客户姓名列表（最多显示2个）
   - 点击单元格可查看详情

#### 月视图测试
1. 点击"月视图"按钮
2. 验证显示当前月份的日历视图
3. 检查是否显示：
   - 每天的排班数量
   - 客户姓名列表（最多显示2个）
   - 点击日期可查看详情

### 3. 数据显示验证
- **排班卡片**: 检查是否显示客户姓名和服务名称
- **统计信息**: 验证今日总数、待排班、急单、已锁定数量
- **排班待办**: 检查待排班预约列表
- **资源筛选**: 测试护士和房间筛选功能

### 4. 交互功能测试
- 点击排班卡片，验证编辑对话框打开
- 点击"分配资源"按钮，验证创建排班对话框打开
- 测试日期选择器功能
- 测试资源筛选器功能

### 5. 错误检查
1. 打开浏览器开发者工具（F12）
2. 查看Console标签页
3. 检查是否有JavaScript错误
4. 查看Network标签页
5. 检查API请求是否正常

## 代码实现验证

### 视图模式实现
从代码分析中确认：

1. **日视图** ✅
   - 时间轴显示（08:00-18:00）
   - 房间和护士排班
   - 排班卡片显示客户和服务信息

2. **周视图** ✅
   - 7天网格布局
   - 房间维度排班汇总
   - 护士维度排班汇总
   - 客户姓名显示（最多2个）

3. **月视图** ✅
   - 月历布局
   - 每日排班汇总
   - 客户姓名显示（最多2个）

### 数据显示实现
- **客户姓名**: `appointment?.customer_name`
- **服务名称**: `appointment?.service?.name`
- **时间显示**: `scheduled_time_start` 和 `scheduled_time_end`
- **急单标识**: `appointment?.is_urgent`

### 组件结构
- **主组件**: `HeadNurseSchedulePage`
- **甘特图**: `GanttChart`（支持三种视图模式）
- **视图切换**: `ViewSwitcher`
- **日期选择**: `DateRangePicker`
- **资源筛选**: `ResourceFilter`

## 已知问题和建议

### 潜在问题
1. API路由可能没有正确配置（测试显示API请求返回HTML而不是JSON）
2. 需要确保数据库中有测试数据
3. 需要验证认证系统是否正常工作

### 测试建议
1. 先确保有测试数据（预约、排班、护士、房间）
2. 检查用户权限（需要head-nurse角色）
3. 验证API端点是否正确配置
4. 测试空数据状态的显示

## 预期结果
- 三种视图模式都能正常切换和显示
- 排班数据正确显示，包含客户姓名和服务名称
- 没有JavaScript错误
- 交互功能正常工作

## 如果发现问题
1. 检查控制台错误信息
2. 验证API响应格式
3. 确认数据库连接和数据
4. 检查用户认证状态