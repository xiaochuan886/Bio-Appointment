# 任务历史页面权限控制使用指南

## 功能概述

任务历史页面现在支持基于用户角色的数据访问权限控制，确保不同角色的用户只能查看其有权限访问的数据。

## 权限规则

### 1. 管理员 (super_admin)
- **数据访问权限**: 可以查看全部任务历史数据
- **UI特性**: 
  - 页面标题显示"查看和分析所有历史任务记录"
  - 不显示数据范围选择器
  - 可以查看所有护士和门店的任务数据

### 2. 护士长 (head_nurse)
- **数据访问权限**: 可以选择查看自己的数据或门店的数据
- **UI特性**:
  - 显示数据范围选择器，包含两个选项：
    - "我的任务": 只查看分配给护士长本人的任务
    - "门店任务": 查看护士长所在门店的所有任务
  - 页面标题根据选择动态更新：
    - 选择"我的任务": "查看和分析您的历史任务记录"
    - 选择"门店任务": "查看和分析门店的历史任务记录"

### 3. 护士 (nurse)
- **数据访问权限**: 只能查看分配给自己的任务
- **UI特性**:
  - 页面标题显示"查看和分析您的历史任务记录"
  - 不显示数据范围选择器
  - 只显示分配给护士本人的任务

## 使用方法

### 护士长用户操作步骤

1. **登录系统**: 使用护士长账号登录
2. **进入任务历史页面**: 点击导航菜单中的"任务历史"
3. **选择数据范围**: 
   - 在页面顶部的筛选栏中，找到数据范围选择器
   - 选择"我的任务"查看个人任务，或选择"门店任务"查看门店所有任务
4. **查看数据**: 页面会根据选择自动刷新并显示相应的数据

### 数据筛选逻辑

#### API层面筛选
- **管理员**: 不添加任何筛选条件，返回所有数据
- **护士长(个人)**: 添加 `nurse_id` 筛选条件
- **护士长(门店)**: 添加 `store_id` 筛选条件  
- **护士**: 添加 `nurse_id` 筛选条件

#### 前端层面验证
为了确保数据安全，前端还会进行二次验证：
- 检查返回的每条记录是否符合用户的访问权限
- 过滤掉不应该显示给当前用户的数据

## 技术实现

### 权限工具函数

```typescript
// 检查用户是否可以查看所有任务历史数据
canViewAllTaskHistory(user): boolean

// 检查用户是否可以选择任务历史数据范围
canChooseTaskHistoryScope(user): boolean

// 获取用户可以访问的任务历史数据筛选参数
getTaskHistoryFilters(user, scope): { nurse_id?, store_id? }
```

### 数据流程

1. **用户选择数据范围** (仅护士长)
2. **调用权限工具函数** 获取筛选参数
3. **发送API请求** 带上筛选参数
4. **服务端筛选** 返回符合权限的数据
5. **前端二次验证** 确保数据安全
6. **显示数据** 在页面上展示

## 安全考虑

### 多层防护
1. **前端权限检查**: 控制UI显示和用户操作
2. **API参数筛选**: 在请求层面限制数据范围
3. **前端数据验证**: 对返回数据进行二次验证
4. **服务端权限控制**: 数据库层面的访问控制

### 数据隔离
- 护士只能看到自己的任务，无法访问其他护士的数据
- 护士长可以选择查看个人数据或门店数据，但无法跨门店访问
- 管理员拥有全局访问权限，但操作会被记录和审计

## 测试验证

运行测试脚本验证权限控制：

```bash
node test-task-history-permissions.cjs
```

测试覆盖：
- ✅ 管理员权限测试
- ✅ 护士长权限测试（个人和门店范围）
- ✅ 护士权限测试
- ✅ UI场景测试
- ✅ 数据访问模拟测试

## 常见问题

### Q: 护士长看不到门店任务怎么办？
A: 检查护士长账号是否正确设置了 `store_id`，只有设置了门店ID的护士长才能查看门店任务。

### Q: 数据范围选择器不显示怎么办？
A: 确认用户角色是 `head_nurse`，只有护士长角色才会显示数据范围选择器。

### Q: 管理员看不到所有数据怎么办？
A: 检查用户角色是否为 `super_admin`，确保权限配置正确。

## 更新日志

- **2024-12-11**: 实现任务历史页面权限控制功能
  - 添加基于角色的数据访问控制
  - 为护士长添加数据范围选择功能
  - 实现多层安全防护机制
  - 添加权限工具函数和测试脚本