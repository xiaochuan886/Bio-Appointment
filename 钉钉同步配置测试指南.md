# 钉钉同步配置测试指南

## 📋 测试前准备

### 1. 确认用户权限

在开始测试前，请确认：

1. **登录用户必须是超级管理员**
   - 用户角色：`super_admin`
   - 用户状态：`active`（活跃）

2. **检查当前用户信息**
   - 打开浏览器开发者工具（F12）
   - 在 Console 中执行以下命令：
   ```javascript
   // 查看当前用户信息
   const { data: { user } } = await supabase.auth.getUser();
   console.log('用户 ID:', user.id);
   
   // 查看用户角色
   const { data: profile } = await supabase
     .from('profiles')
     .select('role, status, username, full_name')
     .eq('id', user.id)
     .single();
   console.log('用户信息:', profile);
   ```

3. **如果不是超级管理员**
   - 联系系统管理员
   - 或使用超级管理员账号登录

### 2. 准备钉钉应用信息

在钉钉开发者后台准备以下信息：

- **AppKey**：应用的唯一标识（例如：dingxxxxxxxx）
- **AppSecret**：应用密钥（长字符串）
- **AgentId**：应用 ID（数字）
- **CorpId**：企业 ID（例如：dingxxxxxxxx）

## 🧪 测试步骤

### 步骤1：打开配置界面

1. 登录系统
2. 进入"用户管理"页面
3. 点击"钉钉同步"标签
4. 点击"配置"按钮

### 步骤2：填写配置信息

1. **填写基本信息**
   - AppKey：输入钉钉应用的 AppKey
   - AppSecret：输入钉钉应用的 AppSecret
   - AgentId：输入钉钉应用的 AgentId
   - CorpId：输入企业的 CorpId

2. **配置同步选项**
   - 启用同步：开启
   - 启用自动同步：根据需要选择
   - 同步频率：选择"每天"
   - 同步时间：设置为"02:00"
   - 冲突解决策略：选择"以钉钉数据为准"

### 步骤3：保存配置

1. 点击"保存配置"按钮
2. **观察浏览器 Console**
   - 应该看到以下日志：
   ```
   开始保存钉钉配置... {app_key: "...", app_secret: "***", ...}
   当前用户 ID: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
   用户角色: super_admin 状态: active
   现有配置: null 或 {id: "..."}
   创建新配置 或 更新现有配置, ID: ...
   配置创建成功: {...} 或 配置更新成功: {...}
   ```

3. **预期结果**
   - 显示"配置保存成功"的提示
   - 对话框自动关闭
   - 配置面板显示最新的配置信息

### 步骤4：验证配置

1. **重新打开配置界面**
   - 点击"配置"按钮
   - 确认所有字段都已填充之前保存的值

2. **检查数据库**
   - 在 Console 中执行：
   ```javascript
   const { data } = await supabase
     .from('dingtalk_sync_config')
     .select('*')
     .single();
   console.log('数据库配置:', data);
   ```

## 🐛 故障排查

### 问题1：保存按钮一直显示"保存中..."

**检查步骤：**

1. **查看 Console 日志**
   ```
   如果看到：
   - "未登录，请先登录" → 重新登录
   - "只有超级管理员可以配置钉钉同步" → 权限不足
   - "获取用户信息失败" → 数据库连接问题
   ```

2. **检查用户角色**
   ```javascript
   // 在 Console 中执行
   const { data: profile } = await supabase
     .from('profiles')
     .select('role, status')
     .eq('id', (await supabase.auth.getUser()).data.user.id)
     .single();
   console.log('角色:', profile.role, '状态:', profile.status);
   
   // 应该显示：
   // 角色: super_admin 状态: active
   ```

3. **检查网络连接**
   - 打开 Network 标签
   - 查看是否有失败的请求
   - 检查请求的响应内容

### 问题2：保存后配置丢失

**检查步骤：**

1. **验证数据库写入**
   ```javascript
   const { data, error } = await supabase
     .from('dingtalk_sync_config')
     .select('*');
   console.log('配置数据:', data);
   console.log('错误:', error);
   ```

2. **检查 RLS 策略**
   - 确认用户有读取权限
   - 确认 `is_admin` 函数正常工作

### 问题3：无法更新配置

**检查步骤：**

1. **查看更新日志**
   ```
   Console 应该显示：
   更新现有配置, ID: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
   配置更新成功: {...}
   ```

2. **检查 updated_at 字段**
   ```javascript
   const { data } = await supabase
     .from('dingtalk_sync_config')
     .select('updated_at')
     .single();
   console.log('最后更新时间:', new Date(data.updated_at).toLocaleString());
   ```

## ✅ 测试检查清单

### 基本功能测试

- [ ] 能够打开配置对话框
- [ ] 能够填写所有必填字段
- [ ] 能够保存配置（首次创建）
- [ ] 能够更新配置（修改已有配置）
- [ ] 保存成功后显示提示信息
- [ ] 保存成功后对话框自动关闭
- [ ] 重新打开对话框时显示已保存的配置

### 权限测试

- [ ] 超级管理员可以保存配置
- [ ] 非超级管理员无法保存配置（显示权限错误）
- [ ] 未登录用户无法访问配置界面

### 数据验证测试

- [ ] 必填字段不能为空
- [ ] AppKey 格式验证
- [ ] AgentId 格式验证
- [ ] 时间格式验证

### 错误处理测试

- [ ] 网络错误时显示友好提示
- [ ] 权限不足时显示明确错误
- [ ] 数据库错误时显示详细信息
- [ ] 所有错误都有日志记录

### 界面交互测试

- [ ] 开关按钮正常工作
- [ ] 下拉选择框正常工作
- [ ] 时间选择器正常工作
- [ ] 取消按钮正常工作
- [ ] 表单验证提示正常显示

## 📊 测试报告模板

### 测试信息

- **测试日期**：2025-11-30
- **测试人员**：[姓名]
- **测试环境**：[浏览器版本]
- **用户角色**：super_admin

### 测试结果

| 测试项 | 预期结果 | 实际结果 | 状态 | 备注 |
|--------|---------|---------|------|------|
| 打开配置界面 | 正常显示 | ✅ 正常 | 通过 | - |
| 填写配置信息 | 可以输入 | ✅ 正常 | 通过 | - |
| 保存配置 | 保存成功 | ✅ 成功 | 通过 | - |
| 更新配置 | 更新成功 | ✅ 成功 | 通过 | - |
| 权限验证 | 仅管理员可用 | ✅ 正常 | 通过 | - |
| 错误处理 | 显示友好提示 | ✅ 正常 | 通过 | - |

### 发现的问题

1. **问题描述**：[描述问题]
   - **严重程度**：高/中/低
   - **复现步骤**：[步骤]
   - **预期结果**：[预期]
   - **实际结果**：[实际]
   - **截图**：[如有]

### 测试结论

- [ ] 所有测试通过，功能正常
- [ ] 部分测试失败，需要修复
- [ ] 测试未通过，需要重新开发

### 建议

[测试建议和改进意见]

## 🔍 调试技巧

### 1. 启用详细日志

在 Console 中执行：
```javascript
// 启用 Supabase 调试日志
localStorage.setItem('supabase.debug', 'true');

// 刷新页面后，所有 Supabase 操作都会有详细日志
```

### 2. 监控网络请求

1. 打开 Network 标签
2. 筛选 XHR/Fetch 请求
3. 查看请求和响应的详细内容
4. 检查请求头中的认证信息

### 3. 检查数据库状态

```javascript
// 检查当前会话
const { data: { session } } = await supabase.auth.getSession();
console.log('会话信息:', session);

// 检查用户权限
const { data: profile } = await supabase
  .from('profiles')
  .select('*')
  .eq('id', session.user.id)
  .single();
console.log('用户完整信息:', profile);

// 检查配置表
const { data: config, error } = await supabase
  .from('dingtalk_sync_config')
  .select('*');
console.log('配置数据:', config);
console.log('错误信息:', error);
```

### 4. 测试 RLS 策略

```javascript
// 测试是否可以读取配置
const { data: readTest, error: readError } = await supabase
  .from('dingtalk_sync_config')
  .select('*');
console.log('读取测试:', readTest, readError);

// 测试是否可以写入配置
const { data: writeTest, error: writeError } = await supabase
  .from('dingtalk_sync_config')
  .insert({
    app_key: 'test',
    app_secret: 'test',
    agent_id: 'test',
    corp_id: 'test'
  });
console.log('写入测试:', writeTest, writeError);
```

## 📞 获取帮助

如果测试过程中遇到问题：

1. **查看文档**
   - 阅读《钉钉同步功能说明.md》
   - 查看常见问题部分

2. **检查日志**
   - 浏览器 Console 日志
   - Network 请求日志
   - 数据库操作日志

3. **联系技术支持**
   - 提供详细的错误信息
   - 提供 Console 日志截图
   - 说明复现步骤

---

**提示：** 测试时请使用测试环境，避免影响生产数据。
